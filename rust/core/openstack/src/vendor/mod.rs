// mirrors dotnet folder `_LIB` — see PORT_MAP.tsv
//
// Renamed from `lib` to `vendor`: a module named `lib` collides with `lib.rs`.
// Third-party code vendored into the C# tree.
pub mod seven_zip;
